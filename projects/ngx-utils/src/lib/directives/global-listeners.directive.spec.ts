import { GlobalListenersDirective } from './global-listeners.directive';

describe('GlobalListenersDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalListenersDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
